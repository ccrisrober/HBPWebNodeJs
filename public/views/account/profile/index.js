/* global app:true */

(function() {
	'use strict';

	app = app || {};

	/*app.Comment = Backbone.Model.extend({
		idAttribute: '_id',
		url: function() {
			return '/accounts/profile/' + this.id + "/comments/")
		}
	});

	app.CommentsCollection = Backbone.Collection.extend({
		model: app.Comments,
		url: '/accounts/profile/comments/',
		parse: function(results) {

		}
	});

	app.CommentView = Backbone.View.extend({
		el: "#comments",
		template: _.template($("tmpl-showcomments").html() ),
		initialize: function() {

		}
	});*/
}());